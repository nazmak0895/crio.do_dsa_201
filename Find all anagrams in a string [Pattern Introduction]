function compareMaps(map1, map2) {
    if (map1.size !== map2.size) {
        return false;
    }

    for (let [key, value] of map1) {
        if (map2.get(key) != value) {
            return false;
        }
    }
    return true;
}

    // a-z = 26
    // A-Z = 26
    // 0-9 = 10
    // total = 62
    // O(62) = O(1) constant ---> will be the TC


//sliding window concept
//fixed size window
//map comaprison logic


function findAllAnagramsInAString(s, p) {
    let patMap = new Map();//patter window
    for (let i = 0; i < p.length; i++){
        patMap.set(p[i], (patMap.get(p[i]) || 0) + 1);
    }

    let winMap = new Map();//sliding window
    let i = 0, j = 0; //for the sliding window
    let ans = []; //store the starting indices of window
    while (j < s.length) {
        winMap.set(s[j], (winMap.get(s[j]) || 0) + 1);//add right most ch to window
        if (j - i + 1 == p.length) {
            if (compareMaps(winMap, patMap))//if the maps are equal
                ans.push(i);//push the element
            //now shrink the winow
            winMap.set(s[i], winMap.get(s[i]) - 1);//reduce the window size
            if (winMap.get(s[i]) == 0)//check if count of ele has become zero
                winMap.delete(s[i]);
            i++;
        }
        j++;
    }
    return ans; //O(n)
}

//   TC = SC = O(n)



let s = 'bacdgabcda';
let p = 'abcd';
console.log(findAllAnagramsInAString(s, p));
let result = findAllAnagramsInAString(s, p);


console.log(result.length);

TC:
You build a frequency map (patMap) → O(p.length)
while(j < s.length) loop → essentially O(s.length)
Map operations (set, get, delete) happen in O(1) average.
map comparison is O(1) (constant bounded by 62)
Sliding window + constant map comparison
Total Time=O(p)+O(s)=O(n) -> O(n) — Linear time


SC:

patMap stores freq of characters in p
winMap stores freq of characters in current window (size = p.length)
Maximum number of unique characters is bounded (≤ 62) ⇒ O(1) space
Array ans stores indices of valid anagram windows
Worst case: every window is valid → O(n)
Output list storing valid indices
O(1)+O(1)+O(n)=O(n)
